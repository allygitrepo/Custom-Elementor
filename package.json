{
  "name": "lightbuilder",
  "version": "1.0.0",
  "description": "LightBuilder V1 - Self-hosted Visual Website Builder",
  "scripts": {
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  }
}
